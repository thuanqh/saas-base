{
  "name": "gobiz",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/gobiz",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "root": "apps/gobiz",
        "outputPath": "dist/apps/gobiz"
      },
      "configurations": {
        "development": {
          "outputPath": "apps/gobiz"
        },
        "production": {}
      }
    },
    "serve": {
      "executor": "@nrwl/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "gobiz:build",
        "dev": true
      },
      "configurations": {
        "development": {
          "buildTarget": "gobiz:build:development",
          "dev": true
        },
        "production": {
          "buildTarget": "gobiz:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nrwl/next:export",
      "options": {
        "buildTarget": "gobiz:build:production"
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/gobiz/jest.config.ts",
        "passWithNoTests": true
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/gobiz/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
